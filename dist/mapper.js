var mapper = {
  createMap: function(name, spawn_name, sources) {
    console.log(JSON.stringify(sources));
  }, // MAIN
  todofixme: function(spawn_name, length) {

      } // loop
};









module.exports = harvesterV2;
